# Welcome to Aks_ide

## Run on localMachine

### Run on local machine for docker_ide branch (docker is not working)

```shell
  docker volume create postgres_data
```

```shell
  docker-compose up
```